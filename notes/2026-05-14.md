# 2026-05-14 — Git and GitHub basics

## What I did today

- Reviewed the basic Git workflow.
- Created `notes/git-basics.md`.
- Practiced small commits.
- Created `notes/git-practice.md`.
- Used `git status`, `git add`, `git commit`, and `git push`.
- Checked commit history with `git log --oneline`.

## Concepts I learned

### Working directory

    It is a project folder on my computer.

### Staging area

    It is a preparation area before a commit.

### Local repository

    It is a git history stored on my computer.

### Remote repository

    It is a git history stored online, for example on GitHub.

### Commit message

    It is a message that describes what has changed.

### Markdown

    It is a simple text format used for notes and README files.

## Commands I used today

```bash
git status
git add .
git add notes/git-basics.md
git commit -m "Add Git basics notes"
git push
git log --oneline
```

## Problems / errors

None

## What I need to repeat

Nothing

## Mini-test

    1. What is the working directory?

    Answer:
    Working Directory is the project folder on my computer.

    2. What is the staging area?

    Answer:
    It is a preparation area before a commit.

    3. What is the local repository?

    Answer:
    It is history stored locally on my computer.

    4. What is the remote repository?

    Answer:
    It is history stored online, eg. on GitHub

    5. What does `git add .` do?

    Answer:
    It adds all changed files to staging area. 

    6. Why should commits be small and clear?

    Answer:
    To have a clear view of all the changes. If we write too much it will be hard to find what we need.

    7. What does `git log --oneline` show?

    Answer:
    It shows the history of our commits. Each in one line. The oldest in the bottom. Last at the top.

    8. What is a good commit message?

    Answer:
    Short, clear message. For example "Add file_name"

    9. What is Markdown used for?

    Answer:
    Markdown is a simple text format to mark importart parts in out text files. When we add # it becomes blue and easy to find in the text file. 

    10. What is the full Git workflow from editing a file to GitHub?

    Answer:
    Editing, Checking Status, Staging, Commiting, Pushing to GitHub.